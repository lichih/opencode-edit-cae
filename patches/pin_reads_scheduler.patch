diff --git a/packages/opencode/migration/20260322045901_add_session_metadata/migration.sql b/packages/opencode/migration/20260322045901_add_session_metadata/migration.sql
new file mode 100644
index 000000000..1f8fcaf64
--- /dev/null
+++ b/packages/opencode/migration/20260322045901_add_session_metadata/migration.sql
@@ -0,0 +1 @@
+ALTER TABLE `session` ADD `metadata` text;
diff --git a/packages/opencode/src/cli/cmd/tui/routes/session/sidebar.tsx b/packages/opencode/src/cli/cmd/tui/routes/session/sidebar.tsx
index 42ac5fbe0..e0447432f 100644
--- a/packages/opencode/src/cli/cmd/tui/routes/session/sidebar.tsx
+++ b/packages/opencode/src/cli/cmd/tui/routes/session/sidebar.tsx
@@ -25,6 +25,7 @@ export function Sidebar(props: { sessionID: string; overlay?: boolean }) {
     diff: true,
     todo: true,
     lsp: true,
+    pinned: true,
   })
 
   // Sort MCP servers alphabetically for consistent display order
@@ -266,6 +267,48 @@ export function Sidebar(props: { sessionID: string; overlay?: boolean }) {
                 </Show>
               </box>
             </Show>
+            <Show when={Object.keys(session().metadata?.pinnedFiles ?? {}).length > 0}>
+              <box>
+                <box
+                  flexDirection="row"
+                  gap={1}
+                  onMouseDown={() =>
+                    Object.keys(session().metadata?.pinnedFiles ?? {}).length > 2 &&
+                    setExpanded("pinned", !expanded.pinned)
+                  }
+                >
+                  <Show when={Object.keys(session().metadata?.pinnedFiles ?? {}).length > 2}>
+                    <text fg={theme.text}>{expanded.pinned ? "▼" : "▶"}</text>
+                  </Show>
+                  <text fg={theme.text}>
+                    <b>Pinned Files</b>
+                  </text>
+                </box>
+                <Show
+                  when={Object.keys(session().metadata?.pinnedFiles ?? {}).length <= 2 || expanded.pinned}
+                >
+                  <For each={Object.values(session().metadata?.pinnedFiles ?? {})}>
+                    {(item: any) => (
+                      <box flexDirection="row" gap={1} justifyContent="space-between">
+                        <text
+                          fg={
+                            item.score > 60
+                              ? theme.success
+                              : item.score > 20
+                                ? theme.warning
+                                : theme.error
+                          }
+                          wrapMode="none"
+                        >
+                          • {path.basename(item.path)}
+                        </text>
+                        <text fg={theme.textMuted}>{item.score}</text>
+                      </box>
+                    )}
+                  </For>
+                </Show>
+              </box>
+            </Show>
           </box>
         </scrollbox>
 
diff --git a/packages/opencode/src/session/index.ts b/packages/opencode/src/session/index.ts
index 0879fe87f..fa8e886d7 100644
--- a/packages/opencode/src/session/index.ts
+++ b/packages/opencode/src/session/index.ts
@@ -149,6 +149,7 @@ export namespace Session {
         archived: z.number().optional(),
       }),
       permission: PermissionNext.Ruleset.optional(),
+      metadata: z.record(z.string(), z.any()).optional(),
       revert: z
         .object({
           messageID: MessageID.zod,
@@ -399,6 +400,27 @@ export namespace Session {
     },
   )
 
+  export const updateMetadata = fn(
+    z.object({
+      sessionID: SessionID.zod,
+      metadata: z.record(z.string(), z.any()),
+    }),
+    async (input) => {
+      return Database.use((db) => {
+        const row = db
+          .update(SessionTable)
+          .set({ metadata: input.metadata, time_updated: Date.now() })
+          .where(eq(SessionTable.id, input.sessionID))
+          .returning()
+          .get()
+        if (!row) throw new NotFoundError({ message: `Session not found: ${input.sessionID}` })
+        const info = fromRow(row)
+        Database.effect(() => Bus.publish(Event.Updated, { info }))
+        return info
+      })
+    },
+  )
+
   export const setArchived = fn(
     z.object({
       sessionID: SessionID.zod,
diff --git a/packages/opencode/src/session/processor.ts b/packages/opencode/src/session/processor.ts
index 38dac41b0..fa2f11a90 100644
--- a/packages/opencode/src/session/processor.ts
+++ b/packages/opencode/src/session/processor.ts
@@ -334,6 +334,26 @@ export namespace SessionProcessor {
                       start: Date.now(),
                       end: Date.now(),
                     }
+
+                    // Pin-Reads Heartbeat parsing
+                    const heartbeats = Array.from(currentText.text.matchAll(/<pin-heartbeat>(.*?)<\/pin-heartbeat>/g))
+                    if (heartbeats.length > 0) {
+                      const session = await Session.get(input.sessionID)
+                      const pinned = session.metadata?.pinnedFiles as Record<string, any>
+                      if (pinned) {
+                        for (const match of heartbeats) {
+                          const path = match[1].trim()
+                          if (pinned[path]) {
+                            pinned[path].score = Math.min(100, (pinned[path].score || 0) + 40)
+                          }
+                        }
+                        await Session.updateMetadata({
+                          sessionID: input.sessionID,
+                          metadata: { ...session.metadata, pinnedFiles: pinned },
+                        })
+                      }
+                    }
+
                     if (value.providerMetadata) currentText.metadata = value.providerMetadata
                     await Session.updatePart(currentText)
                   }
@@ -418,6 +438,27 @@ export namespace SessionProcessor {
           }
           input.assistantMessage.time.completed = Date.now()
           await Session.updateMessage(input.assistantMessage)
+
+          // Pin-Reads Decay
+          const session = await Session.get(input.sessionID)
+          const pinned = session.metadata?.pinnedFiles as Record<string, any> | undefined
+          if (pinned) {
+            let changed = false
+            for (const path of Object.keys(pinned)) {
+              pinned[path].score = (pinned[path].score || 0) - 20
+              if (pinned[path].score <= 0) {
+                delete pinned[path]
+              }
+              changed = true
+            }
+            if (changed) {
+              await Session.updateMetadata({
+                sessionID: input.sessionID,
+                metadata: { ...session.metadata, pinnedFiles: pinned },
+              })
+            }
+          }
+
           if (needsCompaction) return "compact"
           if (blocked) return "stop"
           if (input.assistantMessage.error) return "stop"
diff --git a/packages/opencode/src/session/prompt.ts b/packages/opencode/src/session/prompt.ts
index 743537f59..4a05427b4 100644
--- a/packages/opencode/src/session/prompt.ts
+++ b/packages/opencode/src/session/prompt.ts
@@ -1329,9 +1329,52 @@ export namespace SessionPrompt {
   }
 
   async function insertReminders(input: { messages: MessageV2.WithParts[]; agent: Agent.Info; session: Session.Info }) {
+    // Cleanup old pinned-reads from history to prevent context bloating
+    for (const msg of input.messages) {
+      msg.parts = msg.parts.filter((p) => {
+        if (p.type === "text" && p.synthetic && p.text.includes("<pinned-files>")) {
+          return false
+        }
+        return true
+      })
+    }
+
     const userMessage = input.messages.findLast((msg) => msg.info.role === "user")
     if (!userMessage) return input.messages
 
+    // Inject pinned files if any
+    const pinned = input.session.metadata?.pinnedFiles as Record<string, { path: string; score: number }> | undefined
+    if (pinned) {
+      const active = Object.values(pinned)
+        .filter((f) => f.score > 0)
+        .sort((a, b) => b.score - a.score)
+
+      let total = ""
+      const QUOTA = 50000
+      for (const f of active) {
+        if (total.length >= QUOTA) break
+        const content = await fs.readFile(f.path, "utf8").catch(() => undefined)
+        if (!content) continue
+
+        let segment = `\n---\nFile: ${f.path}\nScore: ${f.score}\n---\n${content}\n`
+        if (total.length + segment.length > QUOTA) {
+          segment = segment.slice(0, QUOTA - total.length - 50) + "\n...[Truncated due to quota]\n"
+        }
+        total += segment
+      }
+
+      if (total) {
+        userMessage.parts.push({
+          id: PartID.ascending(),
+          messageID: userMessage.info.id,
+          sessionID: userMessage.info.sessionID,
+          type: "text",
+          synthetic: true,
+          text: `<pinned-files>\nBelow are the latest contents of the files you have pinned for monitoring:\n${total}\n</pinned-files>`,
+        })
+      }
+    }
+
     // Original logic when experimental plan mode is disabled
     if (!Flag.OPENCODE_EXPERIMENTAL_PLAN_MODE) {
       if (input.agent.name === "plan") {
diff --git a/packages/opencode/src/session/session.sql.ts b/packages/opencode/src/session/session.sql.ts
index b3229edd1..d4a301526 100644
--- a/packages/opencode/src/session/session.sql.ts
+++ b/packages/opencode/src/session/session.sql.ts
@@ -32,6 +32,7 @@ export const SessionTable = sqliteTable(
     summary_diffs: text({ mode: "json" }).$type<Snapshot.FileDiff[]>(),
     revert: text({ mode: "json" }).$type<{ messageID: MessageID; partID?: PartID; snapshot?: string; diff?: string }>(),
     permission: text({ mode: "json" }).$type<PermissionNext.Ruleset>(),
+    metadata: text({ mode: "json" }).$type<{ pinnedFiles?: Record<string, any> }>(),
     ...Timestamps,
     time_compacting: integer(),
     time_archived: integer(),
diff --git a/packages/opencode/src/tool/registry.ts b/packages/opencode/src/tool/registry.ts
index 3ea242a29..3f54e04cf 100644
--- a/packages/opencode/src/tool/registry.ts
+++ b/packages/opencode/src/tool/registry.ts
@@ -1,3 +1,4 @@
+import { PinReadsTool } from "./pin"
 import { PlanExitTool } from "./plan"
 import { QuestionTool } from "./question"
 import { BashTool } from "./bash"
@@ -105,6 +106,7 @@ export namespace ToolRegistry {
       InvalidTool,
       ...(question ? [QuestionTool] : []),
       BashTool,
+      PinReadsTool,
       ReadTool,
       GlobTool,
       GrepTool,
