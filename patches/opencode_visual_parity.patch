diff --git a/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx b/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
index 7456742cd..e340be950 100644
--- a/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
+++ b/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
@@ -1550,7 +1550,7 @@ function ToolPart(props: { last: boolean; part: ToolPart; message: AssistantMess
         <Match when={props.part.tool === "write"}>
           <Write {...toolprops} />
         </Match>
-        <Match when={props.part.tool === "edit"}>
+        <Match when={props.part.tool === "edit" || props.part.tool === "edit_cae"}>
           <Edit {...toolprops} />
         </Match>
         <Match when={props.part.tool === "task"}>
diff --git a/packages/opencode/src/tool/registry.ts b/packages/opencode/src/tool/registry.ts
index 3ea242a29..ce7eadd1f 100644
--- a/packages/opencode/src/tool/registry.ts
+++ b/packages/opencode/src/tool/registry.ts
@@ -75,11 +75,11 @@ export namespace ToolRegistry {
             worktree: Instance.worktree,
           } as unknown as PluginToolContext
           const result = await def.execute(args as any, pluginCtx)
-          const out = await Truncate.output(result, {}, initCtx?.agent)
+          const outputStr = typeof result === "string" ? result : result.output; const out = await Truncate.output(outputStr, {}, initCtx?.agent)
           return {
-            title: "",
-            output: out.truncated ? out.content : result,
-            metadata: { truncated: out.truncated, outputPath: out.truncated ? out.outputPath : undefined },
+            title: typeof result === "object" ? result.title : "",
+            output: out.truncated ? out.content : outputStr,
+            metadata: { ...(typeof result === "object" ? result.metadata : {}), truncated: out.truncated, outputPath: out.truncated ? out.outputPath : undefined },
           }
         },
       }),
